# الالتزام بالمعايير المؤسسية (Coding Standards · OpenAPI · Build Pipeline)

توثيق التزام مشروع **SEHA HMS** (React SPA + ASP.NET Core .NET 10 + PostgreSQL)
بثلاثة معايير: قواعد البرمجة، قواعد التحقق من OpenAPI، وتعميم تحسينات خط البناء.
كل **انحراف** عن المعيار موثّق صراحةً، وكل بند **غير مُتحقَّق فعليًا** موسوم بوضوح.

---

## 0. قيد التحقق (اقرأه أولًا) — Verification status

> **مهم:** بيئة التطوير الحالية **لا تحتوي .NET SDK** و**الوصول لمغذّيات الحزم
> محجوب (npm/nuget → 403)**، كما أن أدوات الواجهة (`vitest`/`eslint`) غير مثبّتة
> ويتعذّر تثبيتها. لذلك تعذّر تشغيل: `dotnet build` · `dotnet test` ·
> `npm test` · `npm run lint`.
>
> ما جرى **تشغيله فعليًا** هنا: تحقق هيكلي من `openapi.yaml` (Python/pyyaml)،
> و`grep` للتأكد من خلو الكود من Elastic APM.
>
> كل بند موسوم **⏳ PENDING-VERIFICATION** يعني: مُنفّذ في الكود/التهيئة لكن
> **لم يُشغّل بناؤه/اختباره فعليًا** — يجب تشغيله في بيئة فيها .NET 10 SDK +
> Docker + وصول للحزم (Codespaces/CI) قبل ادّعاء التوافق.

---

## 1. ملخّص الالتزام

| البند | الحالة | التفاصيل |
| --- | --- | --- |
| OpenAPI (قواعد التحقق) | ✅ مُنفّذ · تحقق جزئي | `openapi.yaml` مُقوّى؛ 0 Critical / 0 High على المجموعة المفحوصة بـ Python (انظر §2). تشغيل أداة الـ55 قاعدة الرسمية: ⏳ PENDING |
| Elastic APM | ✅ **مُتحقَّق** | `grep` يؤكّد عدم وجود أي حزمة/إعداد APM (§7) |
| .NET 10 (`net10.yaml`) | ✅ مُنفّذ · ⏳ بناء PENDING | `net10.0` + `pipeline/net10.yaml` (لم يُبنَ هنا) |
| Node 22 | ✅ مُنفّذ | `engines.node>=22` + `.nvmrc` |
| تغطية ≥20% (واجهة) | ⏳ PENDING | نطاق القياس وُسّع للمشروع كاملًا؛ النسبة الحقيقية تحتاج تشغيلًا (§4) |
| تغطية ≥20% (خادم) | ⏳ PENDING | Coverlet على كامل الـ assembly؛ تحتاج `dotnet test` (§4) |
| Test Containers | ✅ مُنفّذ · ⏳ تشغيل PENDING | اختبارات تكامل PostgreSQL مؤقتة |
| معايير قواعد البيانات | ⚠️ **افتراض غير مؤكّد** | PostgreSQL افتراض — وثيقة المعيار مشفّرة (§3) — يحتاج تأكيدًا بشريًا |
| EF Migrations (بدل EnsureCreated) | 📝 موثّق · كود مؤجَّل | يحتاج SDK لتوليد الهجرات (§5) |
| مصدر واحد للصلاحيات | ✅ مُنفّذ (مصفوفة) · ربط مؤجَّل | `shared/permissions.json` (§6) |
| ثبات سجل التدقيق (DB) | 📝 مُصمَّم · كود مؤجَّل | triggers تمنع UPDATE/DELETE (§6) |
| تقييد CORS في الإنتاج | 📝 موثّق · كود مؤجَّل | يجب أن يصبح إلزاميًا (§6) |
| Git Flow + Code Review | ✅ موثّق | §5 |
| تسمية React بـ PascalCase | ✅ انحراف موثّق | استثناء مقصود مقابل kebab-case (§5) |

---

## 2. OpenAPI — مواءمة قواعد التحقق (الأولوية القصوى)

أُعيدت كتابة `backend/openapi.yaml` (الإصدار `1.1.0`) لتغطية حواجب الإصدار:

- **servers:** أربع بيئات كاملة (development/testing/staging/production) عبر
  `x-environment`، بدون روابط placeholder (لا `example.com`/`localhost`) ولا
  trailing slashes.
- **كل endpoint محمي** يوثّق `401` و`403` و`429`.
- **Rate limiting:** ترويسات `RateLimit-Limit/Remaining/Reset` على الردود
  الناجحة، و`429` مع `Retry-After`.
- **`POST /reports` (201):** ترويسة `Location` (required، `format: uri`).
- **`422`** لكل `POST/PUT/PATCH` (تحقق دلالي) منفصلة عن `400` (نحوي).
- **Idempotency:** ترويسة `Idempotency-Key` + `409` على العمليات المغيّرة للحالة
  (`POST /reports`، `/submit`، `/audit`، و`PUT`).
- **Conditional/Caching:** `ETag` + `Cache-Control` + `If-None-Match` + `304`
  على الـ GET، و`If-Match` + `412` على التعديلات.
- **Pagination:** `page`/`pageSize` + ترويسات `X-Total-Count`/`X-Page`/
  `X-Page-Size`/`Link` على `/reports`.
- **قيود هيكلية:** `minItems/maxItems` لكل array، `minLength/maxLength` لكل
  string، `minimum/maximum` لكل integer، ووصف لكل operation وكل parameter،
  وtags متّسقة.
- **إضافات مخصّصة:** `x-timeout` لكل operation، و`x-languages` على نصوص اللغة
  الطبيعية (انظر الانحراف أدناه).
- **`default`** بصيغة `application/problem+json`، و`operationId` فريد وURL-safe.

**نتيجة التحقق المُشغّل فعليًا** (سكربت Python على `pyyaml`، مجموعة جزئية من
القواعد):

```
CRITICAL: 0
HIGH: 0
WARN: 23   (كلها x-languages على معرّفات تقنية — انظر الانحراف)
RESULT: PASS (no Critical/High)
```

> ⏳ **PENDING:** هذا تحقق هيكلي بمجموعة فرعية من القواعد، **وليس** أداة الـ55
> قاعدة الرسمية (Spectral/Coderz). يجب تشغيل الأداة الرسمية لتأكيد خلو الـ
> Critical/High نهائيًا.

### انحراف موثّق — `x-languages`
المعيار يطلب `x-languages` لكل string. طبّقناها على **نصوص اللغة الطبيعية فقط**
(الأسماء، التوصيات، الملاحظات، الرسائل…)، و**استثنينا المعرّفات التقنية** التي
ليست نصًا قابلًا للترجمة: `nationalId`، `phone`، أكواد القوائم
(`city/nationality/licenseType`)، `referenceNo`، مفاتيح الفحص، `otp`، رمز الـ
JWT، وترويسات `ETag/If-Match`. السبب: وسم هوية وطنية أو رمز JWT بأنه «ثنائي
اللغة» غير صحيح دلاليًا. **هذا انحراف مقصود وموثّق**؛ إن أصرّ المعيار حرفيًا على
كل string، نزيدها بسطر واحد لكل حقل.

---

## 3. قاعدة البيانات — افتراض غير مؤكّد (لا تخمين)

> ⚠️ **اختيار PostgreSQL افتراض غير مؤكّد.** وثيقة «معايير قواعد البيانات»
> المؤسسية **مشفّرة ولم تُقرأ**، فلم نتمكّن من تأكيد المحرّك أو القواعد المطلوبة.
> **لم يُغيَّر المحرّك**، و**يجب تأكيد بشري قبل أي migration**.

**ما الذي سيتأثّر لو تغيّر المحرّك** (قائمة الأثر):

| العنصر | الأثر عند تغيير المحرّك |
| --- | --- |
| `jsonb` (القيم المركّبة عبر `OwnsOne/OwnsMany().ToJson()`) | SQL Server يستخدم `nvarchar(max)` + `ISJSON`؛ Oracle يختلف — يلزم مراجعة التعيين |
| صورة Testcontainers | `postgres:16-alpine` → صورة المحرّك البديل |
| مزوّد EF (`Npgsql.EntityFrameworkCore.PostgreSQL`) | يُستبدل بمزوّد المحرّك (مثل `Microsoft.EntityFrameworkCore.SqlServer`) |
| سلسلة الاتصال + صيغة البارامترات | تتغيّر |
| triggers ثبات السجل (§6) | بنية الـ trigger تختلف بين المحركات |

**عزل الاعتمادية (مؤجَّل — يحتاج بناء):** التصميم الحالي يعزل وصول البيانات في
`AppDbContext` فقط، ونقاط النهاية تتعامل معه مباشرة. الخطة: إدخال واجهة مستودع
(`IReportRepository`) لإخفاء EF/Npgsql خلف تجريد يسهّل التبديل. **لم يُنفَّذ كودًا**
لأن أي تعديل باكاند يحتاج بناءً لا يتوفّر هنا.

---

## 4. تغطية الاختبارات

### نطاق القياس (مُحدَّث)
- **الواجهة:** وُسّع نطاق `coverage.include` في `vitest.config.ts` من
  مجلدات مختارة إلى **المشروع كاملًا** (`src/**`) مع استثناء المُولَّد/التهيئة/
  ملفات الاختبار — ليطابق ما يقيسه الـ pipeline على المشروع لا على جزء منه.
- **الخادم:** Coverlet يقيس **كامل الـ assembly** افتراضيًا (الاستثناء الوحيد
  `Program`) — مطابق لقياس المشروع كاملًا.

### النسبة الحقيقية
> ⏳ **PENDING:** تعذّر تشغيل `vitest`/`dotnet test` هنا، فلا أستطيع الإبلاغ
> برقم حقيقي. **توقّع موثّق (غير مُقاس):** بعد توسيع النطاق للمشروع كاملًا، تنخفض
> النسبة لأن ~60 مكوّن واجهة غير مغطّاة بعد، وقد تكون **دون 20%** حاليًا.

### للوصول إلى 20% ثم 40% ثم 60%
1. **الواجهة:** اختبارات للمكوّنات النقية (`Button`, `Badges`, `Field`,
   `PassFail`, `BloodChips`) ثم سلوك `ReportForm` عبر `@testing-library/react`.
2. **الخادم:** تغطية كل نقاط النهاية وحالات الخطأ (403/404/400/409/422) في
   اختبارات التكامل، واختبارات `Mapping`.
3. رفع عتبات `thresholds` تدريجيًا مع كل مرحلة (20→40→60).

---

## 5. معايير البرمجة

### Git Flow (سياسة الفروع)
- `main`: إصدارات مستقرة قابلة للنشر فقط.
- `develop`: فرع التكامل.
- `feature/*`: ميزة لكل فرع، تُدمج في `develop` عبر PR.
- `release/*`: تثبيت ما قبل الإصدار (إصلاحات فقط) → `main` + وسم نسخة.
- `hotfix/*`: إصلاح عاجل من `main` ثم دمج عكسي في `develop`.

### سياسة مراجعة الكود (Code Review)
- كل تغيير عبر **Pull Request**؛ لا دفع مباشر إلى `main`/`develop`.
- **مراجعة واحدة على الأقل** + اجتياز CI (بناء + اختبار + تغطية + lint + OpenAPI)
  قبل الدمج.
- يُفضَّل **Squash merge** لتاريخ نظيف، ورسائل commit وصفية.

### EF Migrations — بديل معتمد عن Liquibase/Flyway
المعيار يذكر Liquibase/Flyway لإدارة المخطط. **نعتمد EF Core Migrations** كبديل
مكافئ (إصدارات مخطط مُتتبَّعة في الكود + `Database.Migrate()` عند النشر).
> 📝 **مؤجَّل (يحتاج بناء):** التحويل من `EnsureCreatedAsync` إلى Migrations يحتاج
> `dotnet ef migrations add Initial` وهو غير ممكن بلا SDK. مسار التطوير المبسّط
> (`EnsureCreated`) يبقى للاختبار المحلي فقط.

### تسمية مكوّنات React — انحراف موثّق
المعيار يطلب `kebab-case` لأسماء الملفات/المكوّنات. **نُبقي PascalCase لمكوّنات
React** (`Button.tsx`, `ReportForm.tsx`) لأنه **العُرف الصناعي القياسي** لـ React
(اسم المكوّن = اسم الملف). **انحراف مقصود وموثّق**؛ ملفات غير المكوّنات تتبع
العُرف المعتاد (camelCase للأدوات مثل `format.ts`).

---

## 6. تحسينات تصميمية

### مصدر واحد للحقيقة في الصلاحيات
كان منطق الصلاحيات مكرّرًا (`can()` في الواجهة و`ReportPermissions` في الخادم)
مع خطر الانجراف (drift). أُضيف **`shared/permissions.json`** كمصفوفة صلاحيات
واحدة (الأدوار × الإجراءات + قواعد حالة المورد) لتكون المصدر المرجعي.
> 📝 **مؤجَّل (يحتاج بناء):** توليد أنواع TS من الملف وربط `ReportPermissions`
> بقراءته يحتاج تعديل كود يُبنى لاحقًا؛ حاليًا الملف هو **المرجع الموثّق** ويجب
> أن يبقى الكودان متّسقين معه إلى حين الربط الآلي.

### ثبات سجل التدقيق (Timeline) على مستوى DB
بدل «ثبات بالاتفاق»، الخطة: **triggers في PostgreSQL تمنع `UPDATE`/`DELETE`** على
صفوف الـ timeline (أو فصلها في جدول append-only مستقل بصلاحيات `INSERT` فقط).
> 📝 **مؤجَّل (يحتاج بناء/migration):** يُطبَّق ضمن أول EF migration؛ يحتاج SDK.

### تقييد CORS في الإنتاج — يجب أن يصبح إلزاميًا
السياسة الحالية متساهلة (تطوير). **متطلب:** في الإنتاج يُقيَّد المصدر على نطاق
الـ SPA فقط عبر التهيئة (لا «اختياري»).
> 📝 **مؤجَّل (يحتاج بناء):** تعديل `Program.cs` لقراءة المصادر المسموحة من
> التهيئة ورفض ما عداها في الإنتاج.

---

## 7. Elastic APM — مُتحقَّق فعليًا

نتيجة `grep` المُشغّلة على المستودع:

```
.csproj (PackageReference): none
appsettings*.json:          none
أي .cs / .json / .yaml:      none
```

✅ لا توجد أي حزمة `Elastic.Apm.*` ولا أي إعداد APM. الحقن تلقائي على مستوى
الصورة كما يقتضي التعميم، ولا بقايا تسبّب تضاربًا.

---

## 8. سجل الانحرافات (Deviations Register)

| # | الانحراف | المعيار | المبرّر |
| --- | --- | --- | --- |
| D1 | `x-languages` على نصوص اللغة الطبيعية فقط | OpenAPI: كل string | المعرّفات/الرموز ليست نصًا قابلًا للترجمة |
| D2 | EF Migrations بدل Liquibase/Flyway | Coding Standards | بديل مكافئ ومتوافق مع .NET |
| D3 | PascalCase لمكوّنات React | kebab-case | عُرف صناعي قياسي لـ React |
| D4 | PostgreSQL | غير مؤكّد (وثيقة مشفّرة) | افتراض بانتظار تأكيد بشري |

---

## 9. سجل ما يحتاج تحقّقًا (Pending Verification)

| البند | كيف يُتحقَّق |
| --- | --- |
| `dotnet build` / `dotnet test` + تغطية الخادم | بيئة فيها .NET 10 SDK + Docker |
| `npm test` + تغطية الواجهة (النسبة الحقيقية) | تثبيت deps في بيئة بوصول للحزم |
| `npm run lint` | كأعلاه |
| أداة OpenAPI الرسمية (55 قاعدة) | تشغيل Spectral/Coderz على `openapi.yaml` |
| EF Migrations + triggers الثبات | بعد توليد الهجرات في بيئة بناء |
