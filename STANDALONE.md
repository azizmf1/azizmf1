# خدمة التقارير الطبية لرخص الصيد — نسخة مستقلة (Standalone)

هذه نسخة **مفصولة عن منصة صحة**: الخدمة قائمة بذاتها، والدخول يودّي مباشرة إلى
**قائمة التقارير** بدون المرور بدليل الخدمات أو لوحة صحة العامة.

## ما الذي تغيّر عن النسخة الأصلية (الفصل)

| العنصر | قبل | بعد |
| --- | --- | --- |
| بعد تسجيل الدخول (+OTP) | لوحة صحة `/dashboard` | **قائمة التقارير `/hunting-medical` مباشرة** |
| دليل الخدمات `/services` | موجود (كتالوج خدمات صحة) | **مُزال** |
| لوحة صحة العامة `/dashboard` | موجودة (إحصائيات + خدمات المنصة) | **مُزالة** |
| شريط التنقّل الجانبي | عناصر منصة صحة (النظام، التعاميم، إدارة الخدمات، الخدمات) | **عناصر الخدمة فقط**: التقارير · تقرير جديد |
| روابط التنقّل (breadcrumbs) | تبدأ بـ «الرئيسية/الخدمات» في صحة | **تبدأ بالخدمة** |

الوظائف الأساسية كما هي: تسجيل الدخول + OTP، قائمة التقارير (فلاتر/ترقيم)،
إنشاء/تعديل، تدقيق (اعتماد/إعادة)، عرض + سجل الإجراءات، وطباعة A4.

> ملاحظة: أُبقي على الهوية البصرية (شعار/ألوان صحة) كما هي — لم يُطلب تغيير
> العلامة. لو تبي إزالة هوية صحة بالكامل وإعطاءها هوية مستقلة، هذا تعديل منفصل.

## التشغيل

```bash
npm install
npm run dev        # http://localhost:3000  → يفتح مباشرة على قائمة التقارير بعد الدخول
```

الحسابات التجريبية: `doctor` / `auditor` / `admin` — كلها كلمة المرور والرمز `1234`.

الخادم (.NET 10) في مجلد `backend/` كما هو — انظر `backend/README.md`.
